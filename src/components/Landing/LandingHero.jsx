import Hero from "./Hero";

/**
 * LandingHero wrapper component
 * Renders the upgraded rich Hero component for backwards compatibility.
 */
function LandingHero() {
    return <Hero />;
}

export default LandingHero;
